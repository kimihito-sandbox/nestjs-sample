{
  "name": "app",
  "version": "0.0.1",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*"
    ],
    "nohoist": [
      "**/@nestjs",
      "**/@nestjs/**"
    ]
  },
  "scripts": {
    "dev": "yarn workspace api start:dev & yarn workspace web dev"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
